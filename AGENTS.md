# Local Agent Rules

- После каждой правки сразу делать коммит.
- При каждом коммите обновлять память в этом же файле (`AGENTS.md`).

## Memory

### 2026-02-19
- В `LOVELACE/editor.html` текст подзадач 2.1/2.2 переведен на три строки формата «Для значения X нужно получить значение Y.» и отображается через `<p>`.
- В `LOVELACE/editor.html` для подзадач с эталоном (`2.1/2.2`) статус «выполнены успешно» показывается только при точном совпадении эталонных тестов и выходов.
- Для `Задание 4 / Подзадача 4.1` в `LOVELACE/editor.html` добавлен текст-напоминание про задачу «второй максимум», а код по умолчанию очищен.
- Для `Подзадач 1.1–1.4` в `LOVELACE/editor.html` добавлены тексты «Проверить выходные данные для значения X.» (11, 25, 6, 14).
- Добавлен локальный файл `LOVELACE/etalon-solutions.local.txt` с эталонами и кодовыми фразами для `2.1/2.2`; путь внесен в `.gitignore`.
- В `LOVELACE/etalon-solutions.local.txt` для `2.1/2.2` добавлены эмодзи-программы решений (`x+4` и `x/2`).

### 2026-02-20
- `section-auth.js` расширен профилями `LOVELACE` (`CLASSIC`/`ROBOT`) и профильной проверкой в `guardPage`; маршрутизация Lovelace переведена через `LOVELACE/index.html`, а `LOVELACE/editor.html` закрыт профилем `CLASSIC`.
- Добавлены `LOVELACE/editor-robot.html`, `LOVELACE/js/lab-shell.js`, `LOVELACE/js/domain-robot.js` и `LOVELACE/js/domain-classic.js`; robot-ветка работает без переменных, с keycap-цифрами, растительными цветами и отдельным DSL команд/предикатов.
- `LOVELACE/js/domain-classic.js` обновлен со stub до полноценного classic-движка (парсер и исполнение команд текущего формата).
- Добавлена локальная robot-справка `dev/lovelace/emoji-robot-reference.local.html`, путь внесен в `.gitignore`; на клиентской robot-странице подтверждено 5 примеров.
- В `LOVELACE/editor-robot.html` убрана панель ручных тестов, добавлена визуализация поля сценария; сценарии переведены в скрытые закодированные blobs, а проверка успеха идет только при запуске через `fixedTestsByScenario` и снимки состояния (`snapshot`) из `LOVELACE/js/domain-robot.js`.
- В robot-ветке (`LOVELACE/editor-robot.html`) добавлен toggle микроанимации движения робота, цветовые подсказки из легенды поля убраны, а примеры усложнены; `LOVELACE/js/domain-robot.js` теперь возвращает трассу шагов (`trace`), и `LOVELACE/js/lab-shell.js` передает лимит `maxTracePoints` в runtime.
- В `LOVELACE/editor-robot.html` скрыт блок `Выходные данные` (панель 5) для robot-ветки; технический контейнер `#testOutputs` сохранен в DOM, чтобы не ломать общий shell.
- В `LOVELACE/editor.html` добавлено `Задание 3` с двумя подзадачами (`3.1`, `3.2`) по аналогии с `2.*`: стартовые программы с циклами, по 3 теста и эталонная проверка через `CODE_PHRASE_UNLOCK_RULES` (без вывода кодовой фразы, `blob` пустой), чтобы проверялись именно целевые выходы.
- В `LOVELACE/editor.html` убраны подсказки про изменение конкретной строки цикла для `3.1/3.2`; в `LOVELACE/etalon-solutions.local.txt` добавлены эталонные эмодзи-решения для `3.*`.
- Полностью ротированы пароли разделов (включая профили `LOVELACE` и `VIGENERE`) под формат сложной устной передачи; в `section-auth.js` обновлены все хэши и профильные соответствия, а `passwords.local.txt` синхронизирован и дополнен (`LOVELACE_CLASSIC`/`LOVELACE_ROBOT`).
- В `dev/lovelace/emoji-gramming-reference.local.html` слиты classic+robot справки в один локальный документ; `dev/lovelace/emoji-robot-reference.local.html` удален локально. В репозитории вся папка `dev` переведена под игнор (`.gitignore: dev/`) и снята с трекинга (`git rm --cached -r dev`).
- Для правила «в гите только клиентское» в `.gitignore` добавлены не-клиентские пути (`.githooks/`, `scripts/`, `**/md-todo.md`, `VIGENERE/draft/`); соответствующие файлы сняты с трекинга через `git rm --cached`.
- По уточнению, `VIGENERE/draft` относится к клиентской легенде: исключение `VIGENERE/draft/` убрано из `.gitignore`, папка возвращена в трекинг. Пароли разделов упрощены до букво-цифрового формата для 5-6 классов и синхронизированы с `section-auth.js`/`passwords.local.txt`.
- В локальном `LOVELACE/etalon-solutions.local.txt` формат для `2.*` и `3.*` изменен: первично указываются «Ответы (выходы)», пары «вход -> ответ» оставлены дополнительным блоком.
- В `LOVELACE/etalon-solutions.local.txt` коды для `2.*` и `3.*` явно помечены как «Эмодзи-программа (решение)»; добавлено уточнение, что начальный код в этом файле не хранится.
- В локальной `dev/lovelace/emoji-gramming-reference.local.html` robot-раздел справки приведен к формату консольной части: детализированы команды/предикаты, добавлены правила синтаксиса, модель выполнения и 5 мини-примеров.
- В `LOVELACE/editor.html` для подзадач `3.1/3.2` включено открытие блока 6 по точному совпадению эталонов (как в `2.*`): добавлены обфусцированные code-phrases через `seed/blob` (`3.1: ЦикЛ#R-пет7Ля`, `3.2: RoBot&Вит0к9`).
- В локальном `LOVELACE/etalon-solutions.local.txt` добавлены кодовые фразы для `3.1/3.2`, чтобы в эталонках были все коды, выдаваемые при решении задач с unlock-логикой (`2.*` и `3.*`).
- В `LOVELACE/editor.html` и `LOVELACE/js/lab-shell.js` для UX синхронизировано общее правило: если у задания одна подзадача (`count === 1`), блок переключателя подзадач скрывается, при этом остается автовыбор `taskKey + ".1"`.
- В `LOVELACE/editor.html` добавлен режим `4.1` с фиксированными тестами из обфусцированного blob: запуск считается по 10 кейсам в памяти, в UI показываются только 3 открытых read-only теста, а для `4.1` скрыты/заблокированы кнопки изменения списка тестов.
- В `LOVELACE/editor.html` для `4.1` добавлено открытие блока 6 только при полном совпадении 10/10 по эталонам из blob; фраза читается из того же обфусцированного контейнера данных.
- В локальном `LOVELACE/etalon-solutions.local.txt` добавлен раздел `4.1`: формальное правило, 10 пар вход->ответ, кодовая фраза и рабочая эмодзи-программа решения (файл в ignore).
- В `LOVELACE/editor-robot.html` собран аналогичный набор из 4 заданий под Robot DSL: `1.*` (4 базовые подзадачи с готовым стартовым кодом), `2.*` (2 подзадачи без стартового кода, по 3 сценария), `3.*` (2 подзадачи с циклами и стартовым кодом), `4.1` (1 подзадача, 10 сценариев). Обновлены `ROBOT_SUBTASK_PRESETS`, `ROBOT_SUBTASK_TEXTS`, `taskSubtaskCounts` и obfuscated `ROBOT_SCENARIO_BLOBS` для `task:*`.
- В `LOVELACE/editor.html` уточнен текст `4.1` в блоке задания: формулировка про сумму N чисел и сравнение с эталоном E приведена в точном пользовательском варианте.
- В `LOVELACE/editor-robot.html` на визуальном поле добавлены координаты от левого верхнего угла в keycap-emoji-цифрах; также для robot-примеров (`example:1..5`) и подзадачи `task:1.3` расширены фиксированные сценарии до нескольких полей (obfuscated blobs), чтобы коды с проверками валидировались на разных конфигурациях.
- В `LOVELACE/editor-robot.html` переформулированы тексты `ROBOT_SUBTASK_TEXTS` в задачном стиле («Дойдите до финиша…», «закрасьте клетки…») с конкретными требованиями, синхронизированными с фактическими проверками сценариев.
- В `LOVELACE/editor-robot.html` восстановлен многопольный показ сценариев: теперь рендерятся все видимые эталонные поля (для `4.1` остаются открыты только 3), анимация/переключатель работают по каждому полю отдельно; также обновлены 5 примеров до нарастающей сложности с покрытием движения, покраски, условий, цикла по счетчику, цикла по условию и температурных проверок.
- В `LOVELACE/editor.html` обновлена подзадача `4.1` под новый формат ввода «эталон E, затем числа до нуля»: заменен текст условия, пересобран obfuscated `TASK4_DATA_BLOB` с 10 тестами (3 открытых + 7 скрытых) и новыми эталонами по этой логике; локальный `LOVELACE/etalon-solutions.local.txt` синхронизирован (новые пары вход->ответ и программа-решение).
- Для `4.1` в `LOVELACE/editor.html` усложнена и существенно удлинена кодовая фраза (через пересборку obfuscated `TASK4_DATA_BLOB` без раскрытия в исходнике); локальная эталонка обновлена тем же значением.
- В `LOVELACE/editor-robot.html` подписи координатных осей на поле сделаны явными: угловая ячейка сетки теперь показывает `x/y`, чтобы оси `x` и `y` читались однозначно.
- В `LOVELACE/editor-robot.html` тексты robot-задач очищены от намеков на метод решения: убраны формулировки «используя цикл/измените программу/проверка по полям», оставлены только целевые состояния и конечные цели.
- В `LOVELACE/editor-robot.html` подпись осей усилена до явного формата с направлением: в угловой ячейке отображается `x→` и `y↓` (с tooltip), чтобы не было неоднозначности, где какая ось.
- В `LOVELACE/editor-robot.html` сокращены служебные подписи полей: вместо длинных сообщений теперь компактные метки вида `1/3 · 6x4 · финиш` (или `старт`, `не финиш`, `k/n` в анимации), а общий заголовок сценариев тоже укорочен.
- Для robot `Задание 1` в `LOVELACE/editor-robot.html` убраны текстовые формулировки подзадач (`1.1–1.4` скрывают блок текста), при этом во всех `1.*` эталон теперь требует закраску: `task:1.1` изменен на проверку `finishColor:red`, а стартовый код `1.1` дополнен `🎨🌹`.
- Для robot `Задание 1` требование закраски перенесено с финиша на не-финишные клетки: обновлены стартовые коды `1.1–1.4` и obfuscated сценарии `task:1.1–1.4` (ожидания через `expect.cells[...]`), подтверждено прогоном `ok` по всем тестам `1.*`.
- Для robot `Задание 1` полностью исключен текст подзадач на уровне конфигурации: ключи `1.1–1.4` удалены из `ROBOT_SUBTASK_TEXTS`, чтобы блок текста задания для `1.*` гарантированно оставался скрытым.
- В robot-задачах `2.*`, `3.*`, `4.1` стартовые координаты сценариев перестали быть фиксированными `[0,0]`: пересобраны obfuscated blobs с разными `start`, для `2.2` ожидание закраски стартовой клетки синхронизировано с новыми координатами старта; формулировка `2.2` обновлена без привязки к `(0,0)`.
- Добавлен локальный файл `LOVELACE/etalon-robot-solutions.local.txt` с эталонными эмодзи-решениями всех robot-подзадач (`1.1–4.1`); файл внесен в `.gitignore` и не публикуется в git.
- В robot `Задание 2` переработаны подзадачи `2.1` и `2.2`: убрана логика «красить старт/финиш», добавлены проверки закраски контрольных клеток по пути (через `expect.cells`), обновлены тексты целей подзадач и пересобраны obfuscated сценарии `task:2.1`/`task:2.2`; локальный файл `LOVELACE/etalon-robot-solutions.local.txt` синхронизирован новыми программами.
- В `LOVELACE/editor-robot.html` добавлена выдача кодовых фраз после успешного прохождения robot-задач (`2.1`, `2.2`, `3.1`, `3.2`, `4.1`): фразы заданы в obfuscated `seed/blob` (без открытого текста в исходнике), блок 6 открывается только при полном `ok` по результатам запуска сценария.
- В `LOVELACE/editor-robot.html` для подзадач `task:2.1` и `task:2.2` усложнена расстановка стен (пересобраны obfuscated blobs): убран тривиальный обход по периметру, при этом эталонные решения по-прежнему проходят все 3/3 поля в каждой подзадаче.
- В `LOVELACE/editor-robot.html` robot-кодовые фразы для `2.1`, `2.2`, `3.1`, `3.2`, `4.1` полностью заменены на новый набор (не вариации прежних); пересчитаны obfuscated `seed/blob`, открытый текст в исходнике не хранится.
- В `LOVELACE/editor-robot.html` robot-кодовые фразы дополнительно усложнены до более запутанного формата (смешение регистров/алфавитов/цифр/символов); blobs пересобраны, открытый текст в клиентском коде не публикуется.
- В `LOVELACE/editor-robot.html` уточнены формулировки `2.1` и `2.2` в `ROBOT_SUBTASK_TEXTS`: тексты приведены в соответствие с эталонной логикой маршрутной закраски (в `2.1` — весь маршрут в 🌹, в `2.2` — вправо 🍀, вниз 🌻).
- По требованию убрана латинская буква `i` из robot-кодовых фраз (`2.1`, `2.2`, `4.1`): обновлены значения и пересобраны obfuscated blobs в `LOVELACE/editor-robot.html`; локальная `LOVELACE/etalon-robot-solutions.local.txt` синхронизирована.
- В `LOVELACE/editor-robot.html` уточнён текст `2.1`: явно указано, что в 🌹 закрашиваются клетки маршрута, кроме стартовой.
- В `LOVELACE/editor-robot.html` формулировки `2.1/2.2` возвращены к модели контрольных клеток (ограниченное количество): `2.1` — одна контрольная клетка 🌹, `2.2` — две контрольные клетки 🍀 и 🌻.
- В `LOVELACE/editor-robot.html` добавлены визуальные метки контрольных точек на поле (`.scenario-control`): метки строятся из `scenario.expect.cells`, поэтому отображение и проверка синхронизированы по одним и тем же координатам/цветам.
- В `LOVELACE/editor-robot.html` переработаны 5 robot-примеров под структуру: (1) базовое перемещение, (2) покраска контрольных точек, (3) проверка стенок через `🔁`, (4) проверка температуры, (5) комбинированный пример со всем функционалом; для `example:3` и `example:4` пересобраны obfuscated сценарии. Прогон через `domain-robot.js` подтвердил `ok` по всем `example:1..5` (3/3 каждый).
- Для robot `task:1.*` в `LOVELACE/editor-robot.html` контрольные точки скрываются до запуска и показываются после запуска: добавлен флаг `task1ControlMarksRevealed`, рендер меток привязан к нему; проверка `expect.cells` не изменялась.
- В `LOVELACE/editor-robot.html` для `task:4.1` убрано ограничение видимости 3/10 (теперь показываются все 10 полей), обновлен текст подзадачи без упоминания конкретного алгоритма, и полностью заменены 10 сценариев на лабиринтные кейсы с проверкой закраски контрольных точек поворота (`expect.cells`).
- В `LOVELACE/editor-robot.html` для `task:4.1` полностью пересобраны 10 лабиринтных полей: старт во всех кейсах перенесён в правую часть поля, контрольные точки поворота заданы через `expect.cells`; эталонная программа для `4.1` обновлена локально до варианта с покраской угла (`🛤🚜`) и проходит `10/10`.
- В `LOVELACE/editor-robot.html` поля `task:3.1` и `task:3.2` пересобраны так, чтобы размеры полей отличались по обоим измерениям; текущие размеры: `3.1 -> 4x2, 5x3, 6x4`, `3.2 -> 7x5, 8x6, 9x7`; эталонные программы `3.1` и `3.2` подтверждены прогоном `3/3`.
- В `LOVELACE/editor-robot.html` устранена прокрутка в робот-полях: для `.scenario-board` и `.scenario-card-board` убран `overflow:auto`, добавлен адаптивный размер клетки через `computeScenarioCellSize()` и CSS-переменные `--scenario-unit/--scenario-gap`, чтобы сетка масштабировалась под ширину карточки без скроллбаров.
- В `LOVELACE/editor-robot.html` полностью пересобран `task:4.1` под правило плотности стен `10–25%`: заменены все 10 obfuscated сценариев, для каждого поля подтверждена доля стен в диапазоне (факт: `14.8–20.0%`), старт остаётся справа, а эталонная программа `4.1` проходит `10/10`.
- В `LOVELACE/editor-robot.html` `Задание 3` переведено на температуру: обновлены стартовые пресеты `3.1/3.2`, тексты подзадач и obfuscated сценарии `task:3.1`/`task:3.2` (включая температурные значения клеток и температурно-зависимые ожидания цветов). Проверка через `domain-robot.js` подтвердила прохождение эталонных решений `3.1` и `3.2` по `3/3`.
- В `LOVELACE/editor-robot.html` переработано `task:4.1` под температурный поиск финиша: убраны требования закраски, текст подзадачи обновлен, а 10 obfuscated сценариев пересобраны с финишем внутри центрального квадрата `3x3` (9 уникальных позиций, одна повторена дважды), при плотности стен `8/49 = 16.3%` на каждом поле. Для `task:4.1` также скрыт визуальный маркер `F` в рендере (по `scenarioKey`), чтобы финиш искался по температуре.
- В `LOVELACE/editor-robot.html` выровнен рендер сценариев: размер клетки зафиксирован единым (`--scenario-unit: 26px`) для всех полей, убран JS-автоскейл клеток, а раскладка карточек переведена на `flex-wrap` (меньше полей в строке вместо сжатия/ломания сетки); карточки и доска теперь рендерятся по фактической ширине без внутренних скроллбаров.
- В `LOVELACE/editor-robot.html` возвращен визуальный маркер `F` для `task:4.1` (и остальных сценариев): скрытие по `scenarioKey` убрано, проверка по-прежнему выполняется только рантаймом по данным сценария, без чтения `F` программой.
- Для `task:4.1` в `LOVELACE/editor-robot.html` температура назначена всем свободным клеткам (0 пропусков): каждая свободная клетка теперь содержит направляющую температуру (`1..8`), финиш помечен `9`, а эталонный проход выполняется без `🟰` — только через сравнения `🔥/🧊` (проверено `10/10` на всех 10 полях).
- В `LOVELACE/editor-robot.html` увеличен единый базовый размер клетки робо-поля с `26px` до `30px` для лучшей читаемости, без возврата динамического ресайза и без изменения логики раскладки/проверок.
- В `LOVELACE/editor-robot.html` базовый размер клетки робо-поля дополнительно увеличен до `34px` по запросу для читаемости; единый фиксированный размер и перенос карточек по строкам сохранены.
- В `LOVELACE/editor-robot.html` рядом с переключателем анимации добавлены кнопки масштаба поля (`−`/`+`) с шагом `2px`: диапазон ограничен `26..42px` (по 4 шага в каждую сторону от базовых `34px`), текущее значение показывается рядом, изменение масштаба мгновенно перерисовывает все видимые поля.
- В `LOVELACE/editor-robot.html` увеличен размер шрифта подписи осей в угловой ячейке (`x→/y↓`): `font-size` поднят до `clamp(8px, 0.32*unit, 12px)` и немного увеличен `line-height`, чтобы стрелки читались заметнее.
- В `LOVELACE/editor-robot.html` упрощены robot-подзадачи `3.1` и `3.2`: движение теперь строится через проверки стен (`🛤/🧱`), а выбор цвета делается по температуре на текущей клетке. Обновлены пресеты кода, формулировки задач и obfuscated сценарии `task:3.1`/`task:3.2`; эталонные программы подтверждены прогоном `3/3` в обеих подзадачах.
- В `LOVELACE/editor-robot.html` обновлены robot-сценарии `task:1.3` и `task:1.4`: для `1.3` все ожидаемые закрашиваемые клетки унифицированы на координате `3:2` во всех 3 полях, а для `1.4` сценарий сдвинут в правый нижний сектор (`size 7x4`, `start 1:2`, `finish 5:3`, ожидаемая закраска `3:2`). Прогон стартовых решений подтвержден (`1.3 -> 3/3`, `1.4 -> 1/1`).
- В `LOVELACE/editor-robot.html` для `task:1.4` изменена целевая закрашиваемая клетка с `3:2` на `4:2`; синхронно обновлен пресет `1.4` (цикл вправо `🔂3️⃣`, затем закраска и выход к финишу). Прогон подтвердил `status=ok`; локальная `LOVELACE/etalon-robot-solutions.local.txt` синхронизирована.
- В `MORSE/question_2.html` начальное состояние поля принудительно изменено на «все единицы» (при разборе payload значения ячеек больше не читаются из токена); интерфейсный текст не добавлялся и не менялся.

### 2026-02-22
- В `MORSE/question_3.html` заменено кодовое слово на более сложное через пересборку зашитого `EMBEDDED_TOKEN` (слово хранится в obfuscated payload, без открытой строки в исходнике).
- Во всех `MORSE/question_1.html`, `MORSE/question_2.html`, `MORSE/question_3.html` вывод истории шагов разделен на два блока: отдельный `Алгоритм` (строка кода) и отдельный `Пошагово` (нумерованный список); добавлен отдельный monospace-стиль для алгоритма с горизонтальной прокруткой.
- Во всех `MORSE/question_1.html`, `MORSE/question_2.html`, `MORSE/question_3.html` рядом с блоком `Алгоритм` добавлена кнопка `Копировать` (копирование в буфер обмена с fallback через `textarea`/`execCommand`), кнопка неактивна при пустой истории и кратко показывает статус `Скопировано`/`Ошибка`.
- В `MORSE/question_3.html` добавлена отдельная кнопка `Копировать слово` для кодового слова; кнопка показывается только вместе с `codeWordBox` после решения задачи (когда поле обнулено и кодовое слово раскрыто).
- Во всех `MORSE/question_1.html`, `MORSE/question_2.html`, `MORSE/question_3.html` блок `Пошагово` сделан фиксированной высоты (`.mono-steps`): панель больше не растет по мере добавления шагов, а после заполнения (примерно 15 строк) начинает прокручиваться внутри.
- `AGENTS.md` оставлен локальным и снят с git-трекинга (entry в `.gitignore` уже существовал).
- В `LOVELACE/index.html` для профиля `ROBOT` отключен автопереход в `editor-robot.html`; вместо этого показывается меню модулей (3 placeholder-пункта + нижний пункт `Программируя улыбкой` -> `editor-robot.html`), по аналогии с меню `VIGENERE`.
- В `LOVELACE/index.html` для robot-меню пункт `Черепашке дайте слово` получил внешний URL (MSHPython); остальные пункты пока оставлены placeholder `#`.
- В `LOVELACE/index.html` обновлена внешняя ссылка пункта robot-меню `Черепашке дайте слово` на новый URL MSHPython.
- В robot-меню `LOVELACE/index.html` добавлен пункт `Как найти программу` сразу после `Черепашке дайте слово` (временный `#` до выдачи ссылки).
- В `LOVELACE/index.html` для пункта robot-меню `Как найти программу` задан внешний URL MSHPython вместо placeholder `#`.
- В `LOVELACE/index.html` для пункта robot-меню `Блочное программирование` задан внешний URL MSHPython вместо placeholder `#`.
- В `LOVELACE/index.html` повторно обновлена ссылка пункта `Черепашке дайте слово` на новый URL MSHPython.
- В `LOVELACE/index.html` повторно обновлена ссылка пункта `Как найти программу` на новый URL MSHPython.
- В `LOVELACE/index.html` повторно обновлена ссылка пункта `Блочное программирование` на новый URL MSHPython.
- В robot-меню `LOVELACE/index.html` под пунктом `Блочное программирование` добавлены кнопки `Распутывая запутанное` (с внешним URL MSHPython) и `Гроссмейстер Черепашка` (временный `#`).
- В `LOVELACE/index.html` обновлена ссылка пункта `Распутывая запутанное` на новый URL MSHPython.
- В `LOVELACE/index.html` повторно обновлена ссылка пункта `Распутывая запутанное` на новый URL MSHPython (версия `uC6Ly4WJDS9D`).
- В `LOVELACE/index.html` убрана подпись `Выберите модуль.` в robot-меню; список модулей поднят вверх (`mt-4`).
- В `LOVELACE/index.html` для пункта robot-меню `Гроссмейстер Черепашка` задан внешний URL MSHPython вместо placeholder `#`.
- В `LOVELACE/index.html` повторно обновлена ссылка пункта `Гроссмейстер Черепашка` на новый URL MSHPython (версия `I1iJhni4oP_D`).
- В `LOVELACE/index.html` еще раз обновлена ссылка пункта `Гроссмейстер Черепашка` на новый URL MSHPython (версия `7Rr2j4BTQ5qo`).

### 2026-02-23
- Создан объединённый файл `LOVELACE/emoji-gramming-combined-for-methodist.html` — объединены `dev/lovelace/emoji-gramming-reference.local.html`, `LOVELACE/etalon-robot-solutions.local.txt`, `LOVELACE/etalon-solutions.local.txt` без паролей/блокировок; файл добавлен в `.gitignore`.

### 2026-02-25
- Папка `VIGENERE/secret_word/` добавлена в `.gitignore` и снята с git-трекинга (`git rm --cached -r`); ссылка «Секретное слово» удалена из `VIGENERE/index.html` (обе версии главной страницы Vigenere через общий файл).
- В `VIGENERE/activity_log/index.html` страница «Журнал активности» переделана из списка скачивания в интерфейс просмотра: слева меню всех `fragment_*.mp4`, справа обычный HTML5 `<video>`-плеер с переключением роликов без `download`-ссылок.
- В `VIGENERE/activity_log/index.html` убрана подпись под заголовком «Журнал активности» (строка про встроенный плеер).
- В `VIGENERE/old_phone/index.html` страница «Старый телефон» переделана в двухколоночный режим: слева эмулятор кнопочного телефона (мультинажатие кнопок `1`/`2`, дисплей и числовой код по соответствию букв кнопкам), справа показ `old_phone.jpg` на странице с ограничением высоты `700px`; автоскачивание удалено.
- В `VIGENERE/message_blogger_3/index.html` страница переделана по принципу журнала активности: меню изображений слева и просмотр справа без скачивания; в `VIGENERE/message_blogger_2/index.html` автоскачивание заменено на показ `message.png`; в `VIGENERE/index.html` пункт «Послание блогеру - 2» открывает изображение во всплывающем окне, а ссылки «Журнал активности» и «Послание блогеру - 3» ведут на их страницы `index.html`.
- В `VIGENERE/draft/index.html` вместо скачивания `video_script.docx` сделан текстовый микро-редактор на `contenteditable` с тулбаром (жирный/курсив/подчеркивание, семейство шрифта, цвет текста и фон текста); по умолчанию загружен текст из docx с скрытым посланием (буквы с цветом текста `#4a86e8` на таком же фоне, открытый текст белый на `#4a86e8`). В `VIGENERE/index.html` пункт `Черновик` переведен на `./draft/index.html` без скачивания.
- В `VIGENERE/message_blogger_3/index.html` добавлено раскрытие выбранного изображения по клику в полноэкранный viewport-modal (закрытие по кнопке/клику вне/ESC); в `VIGENERE/message_blogger_2/index.html` у изображения убрано скругление углов.
- В `VIGENERE/draft/index.html` для скрытых букв черновика добавлен класс `secret-char` и отдельный стиль `::selection` (синий на синем), чтобы секретное сообщение не проявлялось при выделении текста в браузере.
- В `VIGENERE/draft/index.html` поведение `::selection` для всего редактора изменено на наследование текущих цветов (`background/color: inherit`), чтобы при выделении буквы не перекрашивались и сохраняли заданные стили; отдельный override для `secret-char` больше не нужен.
- В `VIGENERE/index.html` ссылка пункта `Старый телефон` обновлена с прямого скачивания `old_phone.jpg` на страницу `./old_phone/index.html` (эмулятор + встроенное изображение).
- В `VIGENERE/message_blogger_3/index.html` увеличен размер встроенного предпросмотра: у `#letterPreview` задана ширина `75vw` с ограничением `max-w-full`, чтобы картинка по умолчанию отображалась крупнее.
- В `VIGENERE/security/index.html` кнопка `Назад` (`.back-menu`) перенесена в левый верхний угол (`position: fixed`) и увеличена (размер шрифта/паддинги); `body` получил увеличенный верхний `padding`, чтобы кнопка не перекрывала контент.
- В `VIGENERE/old_phone/index.html` числовой код эмулятора переведён на отображение последовательности нажатий (например, `CE -> 11122`), убраны подписи `Дисплей`/`Числовой код` и заголовки секций, а кнопки `1/2` сделаны более плотными: текст центрирован, шрифт увеличен, внутренние отступы уменьшены.
- В `VIGENERE/message_blogger_3/index.html` полноэкранный просмотр изображения дополнен стрелками `‹/›` и навигацией клавишами `←/→`; также увеличена рабочая ширина страницы и размер встроенного предпросмотра до `min(75vw, 1000px)` (с `max-w-full`), чтобы картинка была крупной ещё до раскрытия.
- В `VIGENERE/index.html` у изображения `Послание блогеру - 2` во всплывающем окне (главное меню Vigenere) убрано CSS-скругление углов (`rounded-lg`).
- В `VIGENERE/old_phone/index.html` поле числового кода эмулятора исправлено: теперь код вычисляется по текущему тексту как последовательность мультинажатий (`A=1`, `B=11`, `C=111`, `D=2`, `E=22`, `F=222`), а не накапливает историю всех кликов при циклическом переборе буквы.
- В `VIGENERE/draft/index.html` поведение выделения скорректировано: выделение снова видно (полупрозрачный синий фон), но цвет букв сохраняется (`color: inherit`); для `secret-char` оставлен отдельный `::selection` с фоном `#4a86e8`, чтобы скрытые буквы не проявлялись.
- В `VIGENERE/draft/index.html` фон выделения в редакторе установлен точно в `#4a86e8` (цвет букв при выделении сохраняется через `color: inherit`); в правую часть тулбара добавлена кнопка `Сбросить`, восстанавливающая исходный HTML черновика.
- Создан локальный служебный каталог `LOVELACE/emoji-editors-image-catalog.local.html` (в `.gitignore`) для classic+robot редакторов: карточки эмодзи, генерация PNG через canvas из системного emoji-рендера, копирование картинки в буфер и скачивание PNG.
- В `LOVELACE/editor-robot.html` в легенду поля robot-редактора добавлен пункт `Контрольная точка` с символом `●`, чтобы обозначение соответствовало визуальным меткам `.scenario-control` на поле.
- В `LOVELACE/editor-robot.html` подпись пункта легенды для `●` уточнена с `Контрольная точка` на `Контрольная клетка`.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` экранная клавиатура (`#emojiKeyboard`) перенесена в тот же блок, что и `Редактор кода`; отдельная панель клавиатуры убрана, номера последующих блоков сдвинуты для непрерывной нумерации.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` внутри панели `Редактор кода` введена двухколоночная компоновка `editor-workspace`: слева редактор, справа клавиатура; клавиатуре отдана основная ширина (примерно 70- 2026-02-25: В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` внутри панели `Редактор кода` введена двухколоночная компоновка `editor-workspace`: слева редактор, справа клавиатура; клавиатуре отдана основная ширина (примерно 70%), на узких экранах раскладка падает в одну колонку.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` переработана основная компоновка в три колонки (слева aside с примерами/заданиями, центр — редактор+клавиатура, справа — служебные панели запуска/тестов/результатов), существенно уменьшены внешние поля страницы; цифры `.block-num` скрыты в заголовках блоков.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` блок `Примеры и Задания` перестроен по схеме: верхний ряд `Примеры | Задания`, ниже отдельный ряд `Подзадачи`, затем отдельный ряд `Текст задания`; также высота редактора кода увеличена и привязана к вьюпорту (`min-height: clamp(360px, 58vh, 760px)`).
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` кнопка `На главную` (`.back-link`) сделана заметнее: синий фон `#dbeafe`, синий текст/бордер и более выраженный hover (`#bfdbfe`).
- В `LOVELACE/editor.html` поля тестового ввода/вывода (`.stdin-input`, `.output-box`) ужаты по высоте до минимума около 2 строк (`min-height: 60px`); в `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` блок кнопок `Запустить/Сброс` выделен классом `run-controls` и сделан компактнее (меньшие отступы/радиус/размер шрифта) без потери текста кнопок.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` блок запуска сделан более инлайновым: `Запустить/Сброс` и `statusBox` в одной строке (`.run-panel-top`), а панель кодовой фразы перенесена следующей строкой ниже. В `LOVELACE/editor.html` блок тестов ужат в компактную grid-раскладку карточек (`.test-list`), а кнопка удаления теста заменена на эмодзи-кнопку `🗑️` с `aria-label`.
- В `LOVELACE/editor.html` блок `Выходные данные по тестам` приведён к компактной grid-раскладке, как у тестов (`.test-output-grid`/`.test-output-card`): карточки уменьшены, а заголовок результата собран в одну строку (`.test-output-head`) с инлайновой метой (`Успех. Шаги: ...`) и отдельным цветом по статусу (`ok/error/warn`).
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` окончательно скрыты круговые номера `.block-num`: убрано переопределение `display:inline-grid` и добавлен `display: none !important`, чтобы «номерочки-кругляшочки» не отображались в заголовках блоков.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` исправлен шрифтовой клиппинг в инлайновом `statusBox` блока запуска: для `.run-panel-top .status-box` увеличены `min-height` (до 36px) и `line-height` (до 1.3), чтобы не подрезались нижние элементы букв.
- В `LOVELACE/editor.html` результаты тестов (classic) перенесены внутрь карточек тестов: `renderOutputs()` теперь заполняет встроенные блоки `data-inline-output` рядом с соответствующим тестом (инлайновый заголовок `Тест N + meta` с цветом по статусу). Отдельная панель `Выходные данные по тестам` скрыта, контейнер `#testOutputs` сохранён в DOM как технический.
- Для `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` добавлена адаптивная раскладка с 4 режимами (3 desktop + 1 mobile): `Desktop XL` (3 колонки в линию), `Desktop L` (левая+центр, правая колонка уходит отдельной строкой), `Desktop M` (левый aside сверху, ниже две колонки), `Mobile` (одна колонка). Также на средних ширинах адаптируется `editor-workspace` и инлайновый статус запуска.
- В `LOVELACE/editor.html` во встроенном результате теста (inline output внутри карточки теста) добавлена явная подпись `Выходные данные` над блоком результата, чтобы визуально отделить ввод теста от вывода программы.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` блок `Примеры и Задания` слегка ужат по отступам/зазорам (панель left-aside, `switchers`, `switchers-row`, `examples`, `task-grid/subtask-grid`, `task-text-wrap`), чтобы освободить больше места центральному редактору.
- В `LOVELACE/editor.html` для брейкпоинта `Desktop M` (`1041..1279px`) переработана раскладка через `display: contents` у правой колонки: `Запуск` остаётся справа от редактора, `Кодовая фраза` идёт отдельной строкой ниже, `Тесты` занимают всю ширину снизу; `editor-workspace` в этом режиме снова двухколоночный (клавиатура рядом с редактором).
- В мобильном брейкпоинте (`max-width: 1040px`) для `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` уменьшена минимальная высота блока редактора кода (`.code-editor`, `.code-lines`, `.code-input`) до `clamp(220px, 40vh, 360px)`, чтобы поле не занимало слишком много экрана.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` для инлайнового статуса запуска (`.run-panel-top .status-box`) отключено обрезание в одну строку (`nowrap/ellipsis`), включён перенос текста и увеличен `min-height` до 44px, чтобы длинные сообщения умещались по высоте.
- В `LOVELACE/editor.html` для брейкпоинта `Desktop M` (`1041..1279px`) раскладка упрощена до одной колонки после верхнего aside: блок `Редактор + Клавиатура` занимает всю ширину, а правая колонка (`Запуск`, кодовая фраза, тесты) уходит ниже.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` панель `#codePhrasePanel` сделана акцентной при показе: тёплый градиентный фон (`#fff7ed -> #fffbeb`), янтарный бордер и заметный `box-shadow`, чтобы блок `Кодовая фраза` визуально выделялся.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` скрыто отображение строки `Суммарные шаги` (`#stepCount` помечен `hidden`), при этом элемент оставлен в DOM для совместимости с существующей JS-логикой обновления.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` заголовкам добавлены декоративные нижние границы: у верхнего `.title` — более заметная (`2px`), у `.block-title` — тонкая (`1px`), с небольшим `padding-bottom` для визуального отделения.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` заголовкам добавлен цветовой акцент: `classic` — сине-индиговый (`.title`/`.block-title`), `robot` — зелёный; для robot синхронизированы цвета нижних границ заголовков под зелёную палитру.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` обычным заголовкам `h2` также назначен цветовой акцент в палитре режима (classic: сине-индиговый, robot: зелёный) для единообразия со стилями `.title` и `.block-title`.
- Уточнён стиль заголовков блоков в `LOVELACE/editor.html`: `.block-title` больше не перебивает акцентный цвет `h2` тёмным значением, теперь блоковые заголовки classic тоже окрашены в сине-индиговый цвет.

- Оптимизирован формат `## Memory` для токенизатора без изменения смысла записей: память сгруппирована по датам (`### YYYY-MM-DD`), удалено повторение даты в каждой строке, сохранены все события и порядок внутри дат.
- В локальном `LOVELACE/emoji-editors-image-catalog.local.html` исправлен рендер составных эмодзи-токенов в PNG: canvas теперь расширяется по `measureText()` (с паддингами), чтобы широкие последовательности (например `🔥🟰`, `🧊🟰`) не обрезались.
- В локальном `LOVELACE/emoji-editors-image-catalog.local.html` у кнопки скачивания PNG (`↓`) увеличен размер шрифта стрелки и задан `line-height: 1`, чтобы иконка была заметнее без клиппинга.
- В локальном `LOVELACE/emoji-editors-image-catalog.local.html` добавлена кнопка `Скачать ZIP` для каждой категории эмодзи: архив PNG собирается прямо в браузере (клиентская ZIP-сборка без внешних библиотек) и скачивается отдельным файлом категории.
- В локальном `LOVELACE/emoji-editors-image-catalog.local.html` исправлена коллизия имён файлов для keycap-цифр (`0️⃣..9️⃣`) в ZIP: вместо одинаковых имён теперь используются имена по назначению (например `digit_0..digit_9`), и та же схема применена к одиночной загрузке PNG (с префиксами секции/категории).
- В `LOVELACE/js/lab-shell.js` добавлена пасхалка для запуска по Konami-коду в тексте программы (robot/common shell): поддерживаются направления как стрелки/robot-эмодзи и финал `BA` либо `21`; при совпадении по нажатию `Запустить` показывается всплывающее окно.
- В `LOVELACE/editor.html` для classic пасхалка переведена на последовательность нажатий кнопок навигации (`↑↑↓↓←→←→ Backspace Delete` вместо `BA`) с “вооружением” через nav-панель и показом всплывашки при следующем `Запустить`.
- В `LOVELACE/js/lab-shell.js`, `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` действие пасхалок изменено: вместо всплывашки теперь переключается тёмная тема (`body.konami-dark`) с обновлением статуса; для classic и robot добавлены CSS-override стили тёмной палитры.
- Пасхалки в обоих режимах переведены на мгновенное срабатывание по кнопкам интерфейса (без `Запустить`): последовательность вводится через навигационные кнопки `↑↑↓↓←→←→` и кнопки цифр `2, 1`; в `classic` цифры читаются с emoji-цифр, в `robot` — с keycap-цифр (логика в `LOVELACE/editor.html` и `LOVELACE/js/lab-shell.js`).
- Доработана тёмная тема пасхалки в `LOVELACE/editor.html` и `LOVELACE/editor-robot.html`: добавлены расширенные `body.konami-dark`-override стили для кнопок, полей ввода/textarea, клавиатуры, карточек тестов и (в robot) панелей/карточек сценариев и легенды, чтобы не оставались светлые элементы.
- Дополнительно для тёмной темы пасхалки в обоих редакторах (`LOVELACE/editor.html`, `LOVELACE/editor-robot.html`) затемнены нейтральный `statusBox` (не только `ok/error/warn`) и блок `Текст задания` (`.task-text`), чтобы убрать оставшиеся светлые пятна.
- В `LOVELACE/editor.html` для тёмной темы пасхалки добавлен явный цвет текста подписи теста (`.test-label`) и иконок-кнопок (`.icon-btn`), чтобы заголовки `Тест N` в карточках не оставались тёмными на тёмном фоне.
- В `LOVELACE/editor.html` для тёмной темы пасхалки добавлены контрастные цвета для inline-результатов в карточках тестов: подпись `Выходные данные` (`.inline-output-label`) и `.test-output-meta` внутри `.test-item-output.ok/error/warn` больше не остаются тёмными.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` текст в кнопках навигации (`.nav-btn`) явно центрирован (`text-align: center`); для тёмной темы пасхалки добавлен пропущенный цвет заголовка `Навигация` (`.nav-title`), чтобы он не оставался тусклым.
- В `LOVELACE/editor-robot.html` раскладка карточек в блоке `Поле сценария` переведена на grid по принципу тестов classic (`auto-fit/minmax`): `.scenario-board` теперь CSS-grid, а `.scenario-card`/`.scenario-card-board` растягиваются по ширине колонки вместо `fit-content`.
- По уточнению в `LOVELACE/editor-robot.html` возвращён прежний дизайн карточек полей (flex + `fit-content`), а сам блок `Поле сценария` перенесён в правую колонку (справа от редактора). Также robot-клавиатура разделена на отдельные группы `Сравнение` и `Цифры`, доработаны dark-theme цвета служебных надписей/масштаба/легенды/мета полей и типографика кнопок навигации (`.nav-btn`) в `classic` и `robot`; в robot кнопка `Сброс` теперь перерисовывает все поля в стартовое состояние (робот возвращается на старт на всех видимых полях).
- В `LOVELACE/editor-robot.html` убраны боковые поля страницы: у `body` обнулён горизонтальный padding (`padding: 8px 0`), чтобы robot-редактор занимал ширину без левого/правого внешнего отступа.
- Для `LOVELACE/editor-robot.html` навигационный блок клавиатуры перенесён под эмодзи-группы (у `.keyboard-layout` одна колонка), чтобы освобождать ширину под 2 поля сценария в строку; заголовок навигации переименован в `Навигация по коду` через новый параметр `navTitle` в `LOVELACE/js/lab-shell.js`.
- Для `LOVELACE/editor-robot.html` в брейкпоинте `Desktop M` (`1041..1279px`) увеличена доля правой колонки (`.blocks: 0.86fr / 1.14fr`) и уплотнены клавиатура/навигация (меньше `emoji-group`, `emoji-btn`, `nav-btn`, зазоры), чтобы в `Поле сценария` чаще помещались 2 карточки полей в строку.
- Дополнительно для `LOVELACE/editor-robot.html` сильнее ужаты клавиатура и центр: у `editor-workspace` базово увеличена доля редактора кода (`42/58` вместо `30/70`), уменьшены размеры кнопок эмодзи (`36px`, `20px` шрифт) и в `Desktop XL` существенно расширена правая колонка под `Поле сценария` (`minmax(620px, 1.22fr)`), чтобы помещались 2 поля в строку.
- В `LOVELACE/editor-robot.html` смешанная robot-категория клавиатуры (направления + проверки + цвета) разбита на отдельные группы `направления`, `проверки`, `цвета` через разбиение `ROBOT_KEYBOARD_GROUPS`, чтобы блоки клавиатуры были визуально разделены.
- В `LOVELACE/js/lab-shell.js` и `LOVELACE/editor.html` исправлены русские окончания в статусах выполнения (`тест/теста/тестов`, `сценарий/сценария/сценариев`) через helper `pluralRu()`, чтобы убрать формулировки вида `сценарий(ев)` и `тест(а/ов)`.
- В `LOVELACE/js/lab-shell.js` и `LOVELACE/editor.html` упрощены технические статусные фразы `Готово: ...`: вместо канцелярского `успех/предупреждения/ошибки` без склонения теперь выводятся нормальные формы с `pluralRu()` (например, `1 успех, 2 предупреждения, 0 ошибок`).
- В `LOVELACE/js/lab-shell.js` и `LOVELACE/editor.html` исправлено согласование в фразах `все N ... выполнены`: для `1` теперь используется форма без `все` и с глаголом в ед. числе (`Готово: 1 тест выполнен успешно.`), для остальных остаётся множественная форма.
- В `LOVELACE/editor.html` в classic-редакторе набор переменных сокращён до животных (убраны `🕐..🕛` и `🚨` из `VARIABLE_EMOJIS`), а большой смешанный блок команд на клавиатуре разделён на категории (`ввод/вывод`, `арифметика`, `условия/циклы`) через разбиение `KEYBOARD_GROUPS`.
- Уточнена раскладка команд classic-клавиатуры в `LOVELACE/editor.html`: `➰` (остаток от деления) перенесён из блока управления в арифметический блок.

### 2026-02-26
- В `VIGENERE/old_phone/index.html` раскладка эмулятора старого телефона переведена на русские группы по 4 буквы: кнопка `1` теперь `АБВГ`, кнопка `2` — `ДЕЖЗ`, синхронно обновлены `KEYMAP` и коды мультинажатий (`1..1111`, `2..2222`).
- В `VIGENERE/old_phone/index.html` уменьшена общая высота страницы эмулятора старого телефона для снижения вертикальной прокрутки: убран `min-h-screen` у `main`, уменьшены внешние/внутренние отступы и ограничена высота изображения (`max-h` через `vh` + cap для `xl`).
- В `MORSE` вопросы `2` и `3` поменяны местами перестановкой файлов `question_2.html` и `question_3.html` (без изменения логики задач; сменена только нумерация через имена файлов).
- В `MORSE/index.html` после перестановки файлов синхронизировано меню: пункты `Вопрос 2` и `Вопрос 3` поменяны местами по ссылкам (`href`), чтобы подписи на главной вели на корректные задачи.
- В `MORSE/index.html` откатена синхронизация меню для `Вопрос 2/3`: ссылки на главной возвращены к исходному виду (`question_2.html` и `question_3.html` по номерам) по уточнению.
### 2026-03-14
- В `LOVELACE/editor-robot.html` обновлены формулировки подзадач `3.1/3.2`: теперь они явно требуют закраски старта/финиша перед движением.
- В `LOVELACE/editor-robot.html` для `task:3.1` и `task:3.2` добавлены препятствия в сценарии (около 20% стен); пересобраны obfuscated blobs.
- В `LOVELACE/editor-robot.html` начальные коды (presets) для `3.1` и `3.2` намеренно "испорчены" тремя логическими ошибками (неверные пороги, направления, цвета).
- В локальный файл `LOVELACE/etalon-robot-solutions.local.txt` добавлены расшифрованные кодовые фразы для `3.1` и `3.2`.
- В `LOVELACE/editor.html` и `LOVELACE/editor-robot.html` (через `lab-shell.js`) реализовано сохранение кода подзадач в рамках сессии: при переключении между задачами изменённый код теперь не сбрасывается.
- Примеры (Examples) по-прежнему сбрасывают код при выборе.
- В оба редактора добавлена кнопка «Сбросить код» (появляется рядом с заголовком «Код», если у подзадачи есть непустой начальный пресет), позволяющая вернуть код к исходному состоянию.
